# Copy to .env and edit
PORT=4000
JWT_SECRET=your_jwt_secret_here
# To use Postgres instead of SQLite, set DATABASE_URL, e.g.:
# DATABASE_URL=postgres://user:pass@localhost:5432/recipedb
# If DATABASE_URL is not set, the app will use local SQLite file `database.sqlite`.

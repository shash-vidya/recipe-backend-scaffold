{
  "name": "recipe-backend-scaffold",
  "version": "1.0.0",
  "description": "Minimal Recipe Platform backend scaffold (Express + Sequelize). Defaults to SQLite for local run. JWT auth, recipes, reviews, favorites.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}